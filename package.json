{
  "name": "backend-test-todos",
  "version": "1.0.0",
  "description": "for testing backend skills like implementing auth, curd with db integration, may be implementing websockets or server side events(INTERMIDATE BACKEND DEV)",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Macy",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1"
  }
}
